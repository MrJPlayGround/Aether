# Execution

This area will hold the paper-trading adapter and order lifecycle logic.

Planned modules:

- `adapters/` venue-specific execution adapters
- `orders/` order creation, cancel, replace, dedup
- `positions/` position tracking and reconciliation
- `runtime/` local state files and process metadata

